<SearchBar @onSubmit={{this.submitSearch}} @isLoading={{this.search.isLoading}}>
  <SearchBar::Button @icon="search" @isDisabled={{this.this.search.isLoading}} @type="submit"/>
  <SearchBar::Input @value={{this.search.searchQuery}} @placeholder="Search Books Database..."/>
</SearchBar>